# Terraform

Terraform is reserved for production infrastructure once the deployment target is selected.

Keep app-local compose and developer tooling in `docker/` and `infra/monitoring/`; keep cloud resources here.
