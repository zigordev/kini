# Kini web

Next.js browser client for Kini.

```bash
npm run dev -w @kini/web
```

The browser API origin defaults to `http://localhost:3012`. Override it at
build time with `NEXT_PUBLIC_API_BASE_URL`.
