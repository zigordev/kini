# GitHub Governance

Recommended repository settings:

- Protect `main`.
- Require the CI workflow before merge.
- Require conventional commits or squash merge titles that satisfy Commitlint.
- Enable Dependabot or another dependency update process.
- Store production secrets in the deployment platform, not in repository files.
